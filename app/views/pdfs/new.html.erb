<% flash.each do |key, msg| %>
    <%= content_tag :div, msg, :class => [key, " message"], :id => "notice_#{key}" %>
<% end %>
<%= form_for(:attachment_metadata, :url => { :action => "create" }, :html => { :multipart => true }) do |form| %>
    <p>
    <%= file_field_tag 'upload[pdf]' , accept: 'application/pdf' , required: true %>
    </p>
   
    <%= hidden_field_tag :bib_id  , @bibId  %>
    <p>
    	<%= submit_tag "Upload" %>
    </p>
<% end %>

<a href=<%= bibtexs_path%>>General Bibtexs </a>